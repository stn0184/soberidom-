# СобериДом — техническая спецификация (как строим)

Источник требований — `SPEC.md` Блок 1 (user stories) и Блок 4 (экраны);
зачем — `PROJECT_IDEA.md` и `ВИДЕНИЕ_и_ПРИОРИТЕТЫ.md`. Этот файл — только
указатель: реализация уже описана в `SPEC.md`, и дублировать её сюда не
нужно. Отдельного `techspec/` по разделу на файл не заводится, пока
этап не потребует раздела, которого в `SPEC.md` нет.

**Читай нужный раздел, а не документ.** Спека этапа в блоке «Читать»
называет разделы прямо: `SPEC.md §2.6`, `techspec/00-adr.md`. Границы
подразделов — `grep -n '^### 2\.' SPEC.md`, чтение — `sed -n 'A,Bp'`.

## Разделы

| § | Где | Что внутри |
| --- | --- | --- |
| 0 | [techspec/00-adr.md](techspec/00-adr.md) | Закрытые развилки с обоснованием |
| 2 | `SPEC.md` Блок 2 «Data Model» (§2.0–2.8) | Схема: таблицы, RLS, индексы, сиды. Применённые миграции — `supabase/migrations/`, типы — `src/types/database.ts` |
| 3 | `SPEC.md` Блок 3 «API Endpoints» (§3.0–3.16) | Контракты роутов `src/app/api/`, формат ошибок |
| 5 | `SPEC.md` Блок 5 «Business Logic» (§5.1–5.10) | Расчёт сметы, промерзание, раскрой FFD, волны, прораб, линтер контента |
| 6 | `SPEC.md` Блок 6 «Edge Cases» (1–20) | Граничные случаи, обязательные к обработке |
| В | `SPEC.md` Приложение В | Исходный план сборки 1–7; этапы 1–5 собраны, 6–7 идут через `specs/` |

Номера разделов совпадают с номерами блоков `SPEC.md` и не
переиспользуются. Разделы 1 (стек) и 4 (слои) живут в `CLAUDE.md`.

## Правила правки

- **Изменение схемы данных** = файл миграции + правка `SPEC.md` Блок 2
  в том же коммите. Без исключений.
- **Новый инвариант доступа** — в `CLAUDE.md` §«Инварианты» и RLS-политику
  в миграции, а не в комментарий к коду.
- **Противоречие `SPEC.md` и кода** — побеждает `SPEC.md`; если решение
  пересмотрено, правится `SPEC.md` (версия в шапке) и строка в ADR.
- Решения, принятые на этапе и не отражённые в `SPEC.md`, — строкой
  в `techspec/00-adr.md`; накопившиеся правки переносятся в `SPEC.md`
  следующей версией (v1.6 — список в ADR).
