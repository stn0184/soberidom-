# §0. Закрытые развилки (ADR)

Каждая закрытая развилка — строка: что решали, что решили, почему.
Решение не переписывается — если оно пересмотрено, добавляется новая
строка со ссылкой на старую. Строки до 2026-09-17 перенесены из
`HANDOFF.md` §8 — они приняты владельцем по ходу этапов 1–5 и ещё не
внесены в `SPEC.md` (ждут v1.6).

| Развилка | Решение | Почему | Дата |
| --- | --- | --- | --- |
| Middleware в Next.js 16 | `src/proxy.ts`, хелпер в `src/lib/supabase/middleware.ts` | В Next.js 16 `middleware.ts` переименован в `proxy.ts` (подтверждено Context7) | 2026-07-16 |
| Роут сметы витрины | `/api/projects/[slug]/estimate`, параметр — UUID проекта | Next.js не допускает `[slug]` и `[id]` на одном уровне | 2026-07-16 |
| Layout квиза | Квиз в route-группе `(quiz)` с минимальным layout; `/quiz/results` — в `(public)` с полной шапкой | Первые 30 секунд без отвлекающей шапки (UX §1) | 2026-07-16 |
| Карточки конфигуратора | `config_options` + `image_url, human_description, price_hint, is_beginner_choice, beginner_advice` (миграция 010) | Материалы новичку — картинка + цена + «совет», а не радио-кнопки (UX §3) | 2026-07-17 |
| RLS на `countries` | Политики read_all + admin_write (миграция 011) | В исходном SPEC их не было; после включения RLS без политик смета переставала считаться | 2026-07-17 |
| Нумерация миграций | 010 — UX-поля, 011 — RLS countries, 012 — кабинет (user_prices, user_progress, user_expenses), 013 — обзор этапов | Сдвиг относительно SPEC 2.6 из-за UX-этапа | 2026-07-17 |
| Обзорный экран стройки | Карта этапов на хабе `/my/[purchaseId]` (`JourneyMap`), поля `stages.duration_days`, `stages.result_image_url` (013) | ВИДЕНИЕ §2.1, §2.8: вся стройка целиком, заглянуть в любой этап | 2026-07-17 |
| Эталонный проект | Homesteader's Cabin v2 (12×24 фута), лицензия CC с указанием автора | Легально, реальные чертежи, стационарный дом с лофтом | 2026-07-18 |
| PDF-экспорт (US-015) | Перенесён из этапа 5 в этап 6 | Решение владельца — вместе с прорабом | 2026-07-18 |
| Платежи | `ManualProvider` (донат + активация админом), ЮKassa позже | Проверить спрос до интеграции | 2026-07-16 |
| Порядок после вехи 11/11 | Прораб (этап 6) — после первых продаж; до продаж — пункты ВИДЕНИЕ §4 | «3 проекта + продукт, достаточный для продажи» | 2026-07-19 |
| Файл `spec.md` фреймворка | Не заводится; роль играет `SPEC.md` Блоки 1 и 4 | На Windows `spec.md` и `SPEC.md` — один файл | 2026-09-17 |
| Ворота | `node scripts/gate.mjs` с `CHECKS = lint, build` | Это и была привычка проекта «build + lint перед коммитом» | 2026-09-17 |
| Экран начала этапа в конструкторе | Выбор этапа (сайдбар, карта `?stage=`, переход через границу этапа) открывает экран начала этапа; шаг — кнопкой «Начать этап →». Вход на `/build` без `?stage` по-прежнему ведёт на первый незавершённый шаг | ВИДЕНИЕ §2.3: «перед этапом купите» некуда было положить, а тому, кто просто продолжает стройку, лишний экран не нужен (US-007) | 2026-09-17 |
| Роут supplies (SPEC 3.x, v1.6) | `GET /api/my/[purchaseId]/supplies?stage=<uuid>` → `{ data: { materials, tools, currency }, meta: { needRegion } }`; материалы — позиции `calcEstimateDetailed` этого этапа, инструменты — по `stage_codes`; без региона `materials: []` и `needRegion: true` | Экрану нужны BOM и инструменты одного этапа вместе; считать смету без региона нечем, но инструменты показать можно | 2026-09-17 |
| `durationDays` в ответе /build (SPEC §3.8, v1.6) | `BuildStage` получает `durationDays: number \| null` из `stages.duration_days` | Чип «≈ N дн.» в шапке этапа рисуется сразу, до ответа `/supplies`; на карте этапов та же цифра уже есть | 2026-09-17 |
| Инструмент = потребность + варианты | `project_tools` — потребность («Пилить доски»), конкретные ножовка/циркулярка — строки `tool_variants` (миграция 026); старые колонки `recommendation`, `approx_price_minor`, `approx_rent_day_minor`, `alternative` не удаляются, но не читаются и получают DEFAULT | ВИДЕНИЕ §2.4: у каждой работы 2–3 инструмента с объяснением «дешевле и дольше или дороже и быстрее», право выбора у человека | 2026-09-17 |
| Выбор варианта хранится на покупку | `user_tool_choices (purchase_id, tool_id, variant_id)`, `unique (purchase_id, tool_id)`, RLS «своё»; `PUT /api/my/[purchaseId]/tools/choice` проверяет, что вариант принадлежит потребности, а потребность — проекту покупки | Выбор — это решение конкретной стройки, а не свойство проекта; у одного проекта могут быть сотни покупателей | 2026-09-17 |
| Суммы по действующему варианту | Действующий = выбранный → `is_beginner_choice` → первый по `sort`; `buy` и `borrow_or_buy_cheap` идут в «Купить ≈», `rent` — в «Арендовать ≈» как `rent_day_minor × days_needed`; правило вынесено в `src/lib/tools/effective.ts` и используется сервером и клиентом | Цифра не должна врать до первого клика, а пересчёт по выбору обязан совпадать на клиенте и сервере | 2026-09-17 |
