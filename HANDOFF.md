# HANDOFF — СобериДом · состояние проекта

> Живой документ передачи контекста. Обновляется в конце каждого этапа.
> Назначение: любой (человек или AI-агент), открыв этот файл, за 5 минут понимает — что за проект, что готово, что дальше.
> Дата обновления: 2026-07-16 | Текущий статус: Этап 2 закрыт, SPEC поднят до v1.5, начинаем Этап 3.

---

## 1. Что за проект (в двух абзацах)

**СобериДом** — веб-сервис «IKEA-инструкция для постройки каркасного дома своими руками». Человек без опыта стройки вводит параметры (тип дома, площадь, регион, отопление), получает подбор проектов с рекомендацией фундамента по местности, бесплатно смотрит 3D и предварительную смету, а после оплаты получает интерактивный конструктор: пошаговая сборка «возьми доску, отметь красным маркером, отрежь 2440 мм, прибей сюда», раскрой, живая смета, фин-отчёт план/факт, волны доставки, скрипты переговоров с подрядчиками и AI-«Добрый прораб».

Ниша пустая: конкуренты — либо сухие инженерные калькуляторы (kalk.pro) для тех, кто уже разбирается, либо статичные PDF-библиотеки (karkasdom.info), либо лидогенераторы стройкомпаний. Никто не совмещает подбор по местности + конструктор «для того, кто не держал молоток» + AI-сопровождение. География: старт СНГ (RU/KZ/BY), цель — мир.

---

## 2. Методология

Проект строится по курсу «AI-Архитектор», Spec-First Pipeline:
```
Идея → PROJECT_IDEA.md → SPEC.md → CLAUDE.md → сборка через Claude Code по этапам → Git/деплой
```
Роли: пользователь (владелец продукта) — принимает решения, валидирует; ведущий-ассистент — структурирует, генерирует документы, ведёт по шагам; Claude Code — собирает код строго по SPEC.md.

**SPEC.md — единственный источник истины.** При любом противоречии между кодом, CLAUDE.md и SPEC.md — побеждает SPEC.md. Новые идеи не ломают текущий этап: идут в бэклог (раздел 7) и добавляются в SPEC через правки.

---

## 3. Документы проекта (в корне репозитория)

| Файл | Что это | Версия |
|------|---------|--------|
| PROJECT_IDEA.md | Идея, аудитория, 13 функций, монетизация | v1.4 |
| SPEC.md | Полная тех-спецификация: 6 блоков, 19 таблиц, API, экраны, edge cases | v1.5 (is_free, US-015 PDF, display_name+color этапов, тини-хаус, Этап 0) |
| CLAUDE.md | Инструкция для Claude Code: стек, архитектура, правила, порядок работы | синхронизирован со SPEC v1.5 |
| HANDOFF.md | Этот файл — состояние проекта | — |

---

## 4. Технический стек (зафиксирован)

- Frontend: Next.js 16 (App Router, Turbopack), TypeScript strict, Tailwind CSS v4, shadcn/ui, Lucide
- 3D: three + @react-three/fiber + @react-three/drei (клиентские компоненты, dynamic import, ssr:false)
- Backend: Supabase (PostgreSQL 15, Auth email+password, RLS, Storage)
- AI: Anthropic Messages API (claude-sonnet-4-6), только серверные роуты
- Письма: Resend | Платежи этап 1: ManualProvider (донат + активация админом) → ЮKassa позже
- Деплой: Vercel (планируется на этапе 2, урок 12 курса); Supabase Cloud

---

## 5. Инфраструктура и доступы (где что лежит)

- **Репозиторий GitHub:** github.com/stn0184/soberidom- (private). Ветка `main`.
- **Локальная папка:** C:\Projects\dom-konstruktor (вне OneDrive — важно, не переносить в OneDrive).
- **Supabase проект:** имя `soberidom`, ref `ueminrteohxjlaiksgxt`, регион eu-west-1 (Ireland), тариф Free. ВНИМАНИЕ: у пользователя в том же аккаунте есть проект `Newpeople` (Академия) — его НЕ трогать.
- **MCP-серверы Claude Code:** context7 (user scope, connected), supabase (project scope через .mcp.json).
- **Секреты (в .gitignore, не в Git):** .mcp.json (Supabase access token), .env.local (SUPABASE_URL + ANON_KEY).
- **Ключи на будущее (этапы 4–6):** ANTHROPIC_API_KEY, RESEND_API_KEY, CRON_SECRET, NEXT_PUBLIC_DONATE_URL, NEXT_PUBLIC_METRIKA_ID — добавлять в .env.local по мере надобности (шаблон в .env.example).

---

## 6. План сборки по этапам (из SPEC Приложение В)

1. ✅ **Каркас приложения** — Next.js+Tailwind+shadcn, структура, i18n/ru, миграции 2.0–2.3 (7 таблиц), auth-клиенты, экраны login/register/reset/confirm. **ГОТОВО.**
2. ✅ **Контент-ядро** — миграции 005–007 применены (17 таблиц public + Storage-бакеты public-assets/models), админка: Проекты / Этапы и шаги (+Детали) / Материалы и цены / Регионы (CSV-импорт), 15 API-роутов /api/admin/*, линтер контента шага, загрузка файлов в Storage. **ГОТОВО.**
3. ⬜ **Витрина** — каталог, карточка, конфигуратор, calcEstimate, фундамент-рекомендатор, 3D-вьюер, квиз, лендинг, SEO. **← СЛЕДУЮЩИЙ.**
4. ⬜ Покупка — purchases, промокоды, письма Resend, админ-активация, инструкция оплаты.
5. ⬜ Кабинет — build (конструктор+прогресс+офлайн), cutting (FFD), estimate (свои цены, CSV), finance, delivery, tools, PDF-экспорт всего проекта (US-015).
6. ⬜ Прораб и скрипты — foreman SSE + чат, negotiation_scripts + UI, лимиты.
7. ⬜ Полировка — edge cases 1–20, cron, метрика, print-стили, e2e smoke.

Контент флагманского бесплатного проекта (SPEC v1.5) — тини-хаус-трансформер (is_free=true, эталон витрины: жить во время стройки → переделать в баню, мокрая зона заложена заранее) — заводится через админку, параллельно этапам 3–5. Хозблок 3×4 — второй проект наполнения.
Зафиксированное решение: раздел «Скрипты» в админке — на этапе 6, вместе с таблицей negotiation_scripts (миграция 2.7).

---

## 7. Что сделано в коде (детально, для входа в контекст)

**Коммиты:**
- `a9606df` — документация (PROJECT_IDEA, SPEC, CLAUDE, .gitignore)
- `84a84c9` — этап 1: каркас, auth, миграции 001–004
- `a1a58cb` — этап 1: типы базы из применённых миграций
- `e406d0e` — docs: HANDOFF после этапа 1
- `27ea420` — этап 2: контент-ядро — миграции 005–007, админка, Storage (77 файлов)

**База данных (применено к Supabase через SQL Editor, Success):**
- Этап 1: profiles, countries (сид RU/KZ/BY), regions (сид 9 городов с Mt и климат-районами), retailers (сид 8 ритейлеров), materials, material_prices, retailer_skus. Функция is_admin().
- Этап 2: house_projects, config_options, stages, steps, parts, project_tools, step_parts, step_materials, bom_items, foundation_rules (сид 24 правила). Storage-бакеты public-assets и models (mime-whitelist, 20 МБ, чтение всем / запись admin). Итого 17 таблиц, RLS на всех.

**Приложение:**
- Next.js 16.2.10, структура по CLAUDE.md, тема slate+amber-600, шрифт Inter (cyrillic), lang=ru.
- src/proxy.ts (в Next.js 16 middleware.ts переименован в proxy.ts — подтверждено через Context7; хелпер в lib/supabase/middleware.ts).
- Auth-клиенты client.ts/server.ts/middleware.ts (@supabase/ssr), типизированы Database-типом.
- Экраны: /auth/register (кнопка неактивна без согласия ПД, обработка «email занят», экран «Проверьте почту»), /auth/login (маппинг ошибок), /auth/reset, /auth/confirm (обработчик ссылок из писем Supabase — добавлен сверх исходного SPEC, внесён в SPEC v1.3).
- Middleware защищает /my, /settings, /admin → редирект на /auth/login?next=...
- src/types/database.ts сгенерирован из базы.
- Смоук-тест: все auth-страницы 200, /my → 307 на логин. build + lint зелёные.

**Этап 2 (админка):**
- /admin: layout с проверкой role='admin' (не-админу — 404), сайдбар: Проекты / Этапы и шаги / Материалы и цены / Регионы.
- 15 API-роутов /api/admin/* (SPEC 3.16): CRUD projects/stages/steps/parts/materials/prices/regions + import/regions (CSV-upsert). Гард requireAdmin, Zod-схемы зеркалят таблицы (lib/zod/admin.ts), единый формат ошибок SPEC 3.0.
- Линтер контента шага (lib/admin/linter.ts, SPEC 5.10): живая панель в форме шага, бейдж в списке, серверная блокировка публикации проекта.
- Загрузка файлов в Storage из админки (изображения + GLB), inline-редактирование цен, CSV-импорт регионов.
- Типы src/types/database.ts перегенерированы (17 таблиц); lib/admin/types.ts — на сгенерированных типах с сужением jsonb/check-полей.

---

## 8. Открытые вопросы / технический долг

- **Миграция 008 (v1.5):** SPEC v1.5 добавил поля house_projects.is_free, stages.display_name, stages.color — в применённой базе их ещё нет. Создать и применить `008_v15_fields.sql` (ALTER-скрипт в SPEC, начало Блока 2) в начале этапа 3; затем regen типов, обновить админку (форма этапа: display_name+color; учёт is_free в доступе) и словарь линтера (+обвязка, лага, стропила).
- **Роль admin:** владелец назначает себе role='admin' вручную (update profiles ... where id = (select id from auth.users where email='...')); без этого /admin недоступен.
- **Email-шаблоны Supabase:** письма подтверждения регистрации по умолчанию могут не вести на /auth/confirm. Починка — в дашборде Supabase (Auth → Email Templates), шаблон должен использовать token_hash и вести на /auth/confirm. Сделать перед живым тестом регистрации (не блокирует этап 2).
- **MCP Supabase на Windows:** подключается «наполовину» (Claude Code ходит в базу через Management API, не через нативные MCP-инструменты). Не блокирует. Если чинить — заменить в .mcp.json "command":"cmd" на прямой npx.cmd.
- **Токен Supabase:** личный access token лежит в открытом виде в .mcp.json (файл в .gitignore). На будущее — вынести в переменную окружения. Токен, который был случайно показан в чате ранее, при выходе в продакшн — отозвать и пересоздать.
- **Название репозитория** `soberidom-` с лишним дефисом (косметика, можно переименовать в GitHub Settings).

---

## 9. Бэклог (идеи после MVP-скелета — не в текущих этапах)

Генерация индивидуальных проектов по произвольным параметрам; автоподтягивание рыночных цен и наличия из каталогов ритейлеров; пошаговые 3D-анимации сборки; PWA-режим «на стройке»; прогресс-трекер с фотоотчётами; партнёрская программа с базами стройматериалов и доставкой.

**Фаза 2: Вовлечение**
- Геймификация: значки/уровни, план-отчёт дня.
- Telegram-бот как оболочка: пуши по этапам + «Добрый прораб» в чате.
- Фотоотчёты этапов → финальное слайд-шоу «как я построил дом».

---

## 10. Как продолжить работу

1. Открыть VS Code на папке C:\Projects\dom-konstruktor.
2. В терминале: `claude` (подхватит CLAUDE.md и .mcp.json; одобрить supabase MCP при запросе).
3. Прочитать этот HANDOFF.md и SPEC.md.
4. Продолжить со следующего незакрытого этапа (см. раздел 6) — сейчас это **Этап 2**.
5. Правило: собирать строго один этап, в конце — build+lint зелёные, затем коммит вида «этап N: …».
